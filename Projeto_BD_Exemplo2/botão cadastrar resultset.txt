cli.setCodigo(Integer.parseInt(txt_codigo.getText()));
        cli.set(txt_.getText());
        cli.cadastrar();
        
        ResultSet tabela;
        tabela = null;
        
        tabela = cli.consultar();
        DefaultTableModel modelo = (DefaultTableModel) _____________.getModel();
        modelo.setNumRows(0);
        try{    
            do{
                modelo.addRow(new String[]{tabela.getString(1),tabela.getString(2),tabela.getString(3), tabela.getString(4)});
            }
            while(tabela.next());
            
        }catch(SQLException erro)
        {
            JOptionPane.showMessageDialog(null, "Erro ao Preeenceher tabela");
        }